# Telegram
python-telegram-bot==20.7

# AI (Google Gemini - free tier)
google-generativeai

# Database (no version pin — lets pip resolve httpx conflict)
supabase

# Scheduling
APScheduler

# HTTP
aiohttp
feedparser

# Finance data (free, no API key needed)
yfinance

# Utilities
python-dotenv
pytz
